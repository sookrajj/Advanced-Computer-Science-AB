package DataStructures;

@SuppressWarnings({"unchecked"})
public class Circularqueue<T extends Comparable<T>> extends Queue<T> {
    private T[] buffer;
    private int head;
    private int tail;
    private int size;
    private int capacity;

    public void enqueue(Queue que, T element) {

    }
}
